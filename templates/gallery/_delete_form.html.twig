<form style="padding:40px 0" method="post" action="{{ path('gallery_delete', {'id': gallery.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer cette image ?');">
    <input name="_method" type="hidden" value="DELETE">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gallery.id) }}">
            <button class="btn btn-danger">Supprimer l'image</button>
        </form>
    