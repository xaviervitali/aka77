<form style="padding:40px 0" method="post" action="{{ path('blog_article_delete', {'id': blog_article.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer cet article ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog_article.id) }}">
    <button class="btn btn-danger">Supprimer l'article</button>
</form>
